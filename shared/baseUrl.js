// ─── Base URL ─────────────────────────────────────────────────────────────────
// The root URL for the local JSON server that provides campsite, comment,
// partner, and promotion data. Update the IP address to match your machine's
// local network address when running on a physical device.
export const baseUrl = 'http://10.0.0.233:3001/';
